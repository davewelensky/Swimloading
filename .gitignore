# Local / system
.DS_Store
.vercel
Backups/
Deploy_SwimLoading/
archive/
Supabase Snippet Spots directory sorted by name 2.numbers

# Node
node_modules/

# Environment variables — NEVER commit these
.env
.env.local
.env.*.local

# Graphify knowledge graph — generated locally, rebuilt with `npm run graph:refresh`.
# Not tracked: it is a 12 MB derived artefact that would churn on every commit.
graphify-out/
graphify-out.zip
